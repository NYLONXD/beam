{
  "name": "beam-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Short-lived drop box for beam's encrypted project zips (Cloudflare Worker + R2 + Upstash Redis).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node --test \"test/**/*.test.js\""
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
